# Local Assets

Audio recordings, IQ captures, and other large binary inputs are not stored in git for this project.

Place local-only files under `assets/local/` or another ignored path when working through the notebooks.
